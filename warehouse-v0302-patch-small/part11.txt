/div><div id="thresholdRows">${s.thresholds.map((t,i)=>thresholdRow(t,i)).join('')}</div><button class="no-print" onclick="addThresholdRow()">Add Threshold</button></div>`}
+function renderSettings(){const s=structuredClone(state.init?.settings||{rollingMonths:9,eventTypes:[],thresholds:[]});return `<div class="page-head"><div><div class="title">Attendance Policy & Notice Thresholds</div><div class="sub">Policy point assignments are locked to the approved PWADC warehouse attendance policy. Notice thresholds remain configurable.</div></div><div class="actions"><button onclick="go('roster')">Back to Roster</button><button class="primary" onclick="saveSettings()">Save Thresholds</button></div></div><div class="notice"><strong>Policy ${esc(s.policyVersion||state.init?.policyVersion||'')}</strong> · Rolling period: 9 months · Positive attendance: +1 after 40 clean worked days · Maximum bank: +5. Sunday and Mandatory Day rules override ordinary point values rather than stacking.</div><div class="card"><div class="card-title">Approved Attendance Options & Point Assignments</div><div class="table-wrap"><table><thead><tr><th>Code</th><th>Attendance Option</th><th>Policy Rule</th><th>40-Day Effect</th></tr></thead><tbody>${s.eventTypes.map(t=>`<tr><td><strong>${esc(displayCode(t.code))}</strong></td><td>${esc(t.label)}</td><td>${esc(pointRule(t))}</td><td>${t.classification==='worked'?'Counts as worked':t.classification==='pause'?'Pauses, no reset':'Resets to 0'}</td></tr>`).join('')}</tbody></table></div></div><div class="card"><div class="card-title">Point Thresholds & Notice Levels</div><div class="notice">Thresholds use <strong>net attendance points</strong> after earned positive attendance credits are applied.</div><div id="thresholdRows">${s.thresholds.map((t,i)=>thresholdRow(t,i)).join('')}</div><button class="no-print" onclick="addThresholdRow()">Add Threshold</button></div>`}
 function eventTypeRow(t,i){return `<div>${esc(t.label)}</div>`}
 function thresholdRow(t,i){return `<div class="threshold-row" data-threshold-row="${i}"><div><label>Threshold / Notice Level</label><input data-k="label" value="${esc(t.label)}"></div><div><label>Net Points</label><input data-k="minPoints" type="number" step="0.25" value="${Number(t.minPoints||0)}"></div><div><label>Highlight</label><select data-k="level"><option value="attention" ${t.level==='attention'?'selected':''}>Attention</option><option value="elevated" ${t.level==='elevated'?'selected':''}>Elevated</option><opti