trong> now has <strong>${money(state.employee.activeTotal)} net points</strong> and reached <strong>${esc(t.label)}</strong>.</div><div class="modal-actions"><button onclick="closeModal()">Close</button><button class="primary" onclick="closeModal();createNotice('${esc(t.id)}')">Create Notice</button></div>`)}else toast('Attendance event saved.')}catch(e){toast(e.message)}}
@@ -112,7 +124,7 @@ async function exportEmployee(){try{const r=await bridge('export:employee-xlsx',
 async function exportAll(){try{const r=await bridge('export:all-xlsx');toast('All attendance Excel exported: '+r.path)}catch(e){toast(e.message)}}
 async function changeDataRoot(){try{const r=await bridge('data:set-root');if(!r.changed)return;state.init=await bridge('app:init');state.employees=state.init.employees||[];state.selectedEid='';state.employee=null;state.page='daily';await refreshDaily();render();toast('Data folder changed.')}catch(e){toast(e.message)}}
 
-function renderSettings(){const s=structuredClone(state.init?.settings||{rollingMonths:9,eventTypes:[],thresholds:[]});return `<div class="page-head"><div><div class="title">Attendance Policy & Notice Thresholds</div><div class="sub">Policy point assignments are locked to the approved PWADC warehouse attendance policy. Notice thresholds remain configurable.</div></div><div class="actions"><button onclick="go('roster')">Back to Roster</button><button class="primary" onclick="saveSettings()">Save Thresholds</button></div></div><div class="notice"><strong>Policy ${esc(s.policyVersion||state.init?.policyVersion||'')}</strong> · Rolling period: 9 months · Positive attendance: +1 after 40 clean worked days · Maximum bank: +5. Sunday and Mandatory Period rules override ordinary point values rather than stacking.</div><div class="card"><div class="card-title">Approved Attendance Options & Point Assignments</div><div class="table-wrap"><table><thead><tr><th>Code</th><th>Attendance Option</th><th>Policy Rule</th><th>40-Day Effect</th></tr></thead><tbody>${s.eventTypes.map(t=>`<tr><td><strong>${esc(t.code)}</strong></td><td>${esc(t.label)}</td><td>${esc(pointRule(t))}</td><td>${t.classification==='worked'?'Counts as worked':t.classification==='pause'?'Pauses, no reset':'Resets to 0'}</td></tr>`).join('')}</tbody></table></div></div><div class="card"><div class="card-title">Point Thresholds & Notice Levels</div><div class="notice">Thresholds use <strong>net attendance points</strong> after earned positive attendance credits are applied.<