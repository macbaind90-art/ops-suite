ailed/manual entry. Point values are assigned by the approved policy engine and cannot be manually overridden.</div><div class="form-grid"><div><label>Date</label><input id="eventDate" type="date" value="${localDateISO()}"></div><div><label>Event Type</label><select id="eventType">${types.map(t=>`<option value="${esc(t.id)}">${esc(t.code)} · ${esc(t.label)} · ${esc(pointRule(t))}</option>`).join('')}</select></div><div class="full"><label>Notes</label><textarea id="eventNotes" placeholder="Operational note or attendance context"></textarea></div></div><div class="modal-actions"><button onclick="closeModal()">Cancel</button><button class="primary" onclick="addEvent()">Save Attendance Event</button></div>`)}
+function openEventModal(){const types=activeEventTypes();modal(`<div class="modal-head"><div class="modal-title">Add Attendance Event</div><button onclick="closeModal()">Close</button></div><div class="notice">Use this for a detailed/manual entry. Point values are assigned by the approved policy engine and cannot be manually overridden.</div><div class="form-grid"><div><label>Date</label><input id="eventDate" type="date" value="${localDateISO()}"></div><div><label>Event Type</label><select id="eventType">${types.map(t=>`<option value="${esc(t.id)}">${esc(displayCode(t.code))} · ${esc(t.label)} · ${esc(pointRule(t))}</option>`).join('')}</select></div><div class="full"><label>Notes</label><textarea id="eventNotes" placeholder="Operational note or attendance context"></textarea></div></div><div class="modal-actions"><button onclick="closeModal()">Cancel</button><button class="primary" onclick="addEvent()">Save Attendance Event</button></div>`)}
 function syncEventPoints(){}
 function syncActiveUntil(){}
 async function addEvent(){try{const date=document.getElementById('eventDate').value,eventTypeId=document.getElementById('eventType').value,notes=document.getElementById('eventNotes').value.trim();const result=await bridge('event:add',{eid:state.selectedEid,date,eventTypeId,notes});closeModal();await loadEmployee(state.selectedEid,false);await refreshRoster();render();if(result?.jobAbandonmentAlert)showJobAbandonment(state.employee.record.profile.name);const crossed=result?.crossedThresholds||[];if(crossed.length){const t=crossed.at(-1);modal(`<div class="modal-head"><div class="modal-title">Attendance Threshold Reached</div><button onclick="closeModal()">Close</button></div><div class="notice critical"><strong>${esc(state.employee.record.profile.name)}</s