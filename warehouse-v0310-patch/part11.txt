er every negative attendance event.</div>`;const reached=all.filter(t=>total>=Number(t.minPoints)),highest=reached.at(-1),next=all.find(t=>total<Number(t.minPoints));return `<div class="card ${thresholdClass(highest)}"><div class="card-title">Threshold Status</div><div class="two-col"><div><strong>Current:</strong> ${highest?thresholdChip(highest):'<span class="chip">Below first threshold</span>'}<div class="muted" style="margin-top:7px">${next?`${money(Number(next.minPoints)-total)} net point(s) until ${esc(next.label)}.`:'Highest configured threshold reached.'}</div></div><div><div class="notice"><strong>Notice rule:</strong> every negative attendance occurrence automatically creates one notice. Net total 4 or fewer uses <strong>Points Notification</strong>; net total 5 or more uses <strong>Confirming Notice</strong>.</div></div></div></div>`}
+function renderNotices(notices){return `<div class="card"><div class="card-title">Notice History</div><div class="notice">A notice is generated automatically after every point-bearing negative attendance occurrence. <strong>4 net points or fewer:</strong> Points Notification. <strong>5 net points or more:</strong> Confirming Notice.</div><div class="table-wrap"><table><thead><tr><th>Generated</th><th>Notice</th><th>Attendance Event</th><th class="num">Assessed</th><th class="num">Net Total</th><th>Status</th><th>Generated By</th><th class="no-print">Action</th></tr></thead><tbody>${notices.length?notices.slice().sort((a,b)=>String(b.generatedAt).localeCompare(String(a.generatedAt))).map(n=>`<tr class="${n.status==='Voided'?'event-voided':''}"><td>${esc(new Date(n.generatedAt).toLocaleString())}</td><td>${esc(n.title||n.noticeType||'Attendance Notice')}</td><td>${esc(n.triggerEventType||'')}</td><td class="num">${money(n.pointsAssessed||0)}</td><td class="num">${money(n.pointsAtGeneration)}</td><td>${esc(n.status)}${n.voidReason?`<br><small>${esc(n.voidReason)}</small>`:''}</td><td>${esc(n.generatedBy)}</td><td class="no-print"><button class="sm" onclick="printNotice('${esc(n.id)}')">Print</button></td></tr>`).join(''):`<tr><td colspan="8" class="muted">No notices generated.</td></tr>`}</tbody></table></div></div>`}
+function noticePacketHtml(r,n){const p=r.profile,evt=(r.events||[]).find(e=>e.id===n.triggerEventId)||(r.events||[]).find(e=>n.eventIds?.includes(e.id));const generated=new Date(n.generatedAt),dateShort=