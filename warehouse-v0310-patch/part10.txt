ance notice')}</strong> was generated automatically and retained in the employee record.</div>${extra}<div class="modal-actions"><button onclick="closeModal()">Close</button><button class="primary" onclick="printNoticeForEmployee('${esc(row.eid)}','${esc(n.id)}')">Print Notice</button></div>`)}
+async function setDailyEvent(eid,eventTypeId,notes){try{const payload={eid,date:state.dailyDate,eventTypeId};if(notes!==undefined)payload.notes=notes;const result=await bridge('daily:set',payload);replaceDailyRow(result.row);render();if(result.notice)showAutomaticNotice(result.row,result.notice,result.jobAbandonmentAlert);else if(result.jobAbandonmentAlert)showJobAbandonment(result.row.name);else toast(`${result.row.name}: ${result.row.event?.type||'Daily entry'} saved.`)}catch(e){toast(e.message)}}
+async function addEvent(){try{const date=document.getElementById('eventDate').value,eventTypeId=document.getElementById('eventType').value,notes=document.getElementById('eventNotes').value.trim();const result=await bridge('event:add',{eid:state.selectedEid,date,eventTypeId,notes});closeModal();await loadEmployee(state.selectedEid,false);await refreshRoster();render();if(result?.notice){const n=result.notice,job=result?.jobAbandonmentAlert?'<div class="notice critical" style="margin-top:10px"><strong>Possible Job Abandonment:</strong> Management/HR review is required.</div>':'';modal(`<div class="modal-head"><div class="modal-title">${esc(n.title)} Created</div><button onclick="closeModal()">Close</button></div><div class="notice">The attendance event was saved and the required notice was generated automatically.<br><strong>Points assessed:</strong> ${money(n.pointsAssessed)} · <strong>Net total:</strong> ${money(n.pointsAtGeneration)}</div>${job}<div class="modal-actions"><button onclick="closeModal()">Close</button><button class="primary" onclick="printNotice('${esc(n.id)}')">Print Notice</button></div>`)}else if(result?.jobAbandonmentAlert)showJobAbandonment(state.employee.record.profile.name);else toast('Attendance event saved.')}catch(e){toast(e.message)}}
+function renderThresholdPanel(x){const total=Number(x.activeTotal||0),all=(state.init?.settings?.thresholds||[]).filter(t=>t.active).sort((a,b)=>a.minPoints-b.minPoints);if(!all.length)return `<div class="notice warn">Point highlight thresholds are not configured. Attendance notices are generated automatically aft