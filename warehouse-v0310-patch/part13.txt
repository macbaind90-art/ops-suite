have a total of <strong>${pointText(total)}</strong> points.`:`This is to confirm our conversation today during which I communicated that you have been assessed <strong>${pointText(assessed)}</strong> points for your violation of the company’s Absenteeism and Tardiness Policy on ${esc(eventWhen)}, ${esc(eventType)}.<br><br>You currently have a total of <strong>${pointText(total)}</strong> Attendance points.`;let html=`<!doctype html><html><head><meta charset="utf-8"><title>${confirming?'Confirming Notice':'Points Notification'} - ${esc(p.name)}</title>${css}</head><body><div class="page">${voided}${header}<div class="title">${confirming?'Confirming Notice':'Points Notification'}</div><div class="info"><div class="lbl">Date:</div><div>${esc(dateShort)}</div><div class="lbl">To:</div><div>${to}</div></div><div class="from">From: ${esc(from)}</div><div class="subj">Subj: Attendance</div><div class="body">${intro}</div>`;if(confirming)html+=`<div class="rule"></div><div class="warning">Your points record has been attached to this form.</div><div class="rule"></div><div class="warning">This Confirmation Notice serves as a <strong>Written Warning</strong> that your violation of the company’s attendance policy is unacceptable and must not reoccur. Please understand that future instances may result in further action up to and including termination of your employment.</div><div class="rule"></div><div class="receipt">By signing this document, you acknowledge only that you have received it. Your signature does not indicate that you agree or disagree with what has been communicated to you. You are invited to add comments below.</div><div class="sig"><div class="sigline">Employee Signature and Date</div><div class="sigline">Manager Name and Date</div></div><div class="witness sigline">Witness Signature and Date</div></div><div class="page comments-page">${header}<div style="height:.35in"></div><div class="comments">Employee Comments:</div>`;return html+`</div></body></html>`}
+async function printNoticeForEmployee(eid,id){try{const x=await bridge('employee:get',{eid}),n=(x.record.notices||[]).find(v=>v.id===id);if(!n)return toast('Notice not found.');await bridge('print:html',{html:noticePacketHtml(x.record,n)})}catch(e){toast(e.message)}}
