{esc(t.label)} · ${esc(pointRule(t))}" onclick="setDailyEvent('${esc(r.eid)}','${esc(t.id)}')"><strong>${esc(t.code||'ATT')}</strong></button>`).join('')}</div><div class="daily-current">${e?`<span class="daily-current-code">${esc(e.code||'')}</span><div><strong>${esc(e.type)}</strong><small>${money(e.points)} negative pt${Number(e.points)===1?'':'s'}${e.notes?` · ${esc(e.notes)}`:''}</small></div>`:'<span class="muted">No entry</span>'}</div><div class="daily-total"><span>Net Total</span><strong>${money(r.total)}</strong></div><div class="daily-row-actions"><button class="sm" ${e?'':'disabled'} onclick="editDailyNote('${esc(r.eid)}')">Note</button><button class="sm danger" ${e?'':'disabled'} onclick="clearDailyEvent('${esc(r.eid)}')">Clear</button></div></div>`}
 async function changeDailyDate(v){state.dailyDate=v||localDateISO();await refreshDaily();render()}
 async function markAllPresent(){if(!confirm(`Mark every currently blank Daily Entry as Present for ${fmtDate(state.dailyDate)}? Existing entries will not be changed.`))return;try{const r=await bridge('daily:mark-all-present',{date:state.dailyDate});state.daily=r.daily;await refreshRoster();render();toast(`${r.changed} employee(s) marked Present.`)}catch(e){toast(e.message)}}
 async function setDailyEvent(eid,eventTypeId,notes){try{const payload={eid,date:state.dailyDate,eventTypeId};if(notes!==undefined)payload.notes=notes;const result=await bridge('daily:set',payload);replaceDailyRow(result.row);render();if(result.jobAbandonmentAlert)showJobAbandonment(result.row.name);const crossed=result.crossedThresholds||[];if(crossed.length)showDailyThreshold(result.row,crossed.at(-1));else toast(`${result.row.name}: ${result.row.event?.type||'Daily entry'} saved.`)}catch(e){toast(e.message)}}
@@ -52,11 +52,11 @@ async function createNoticeFor(eid,thresholdId){try{closeModal();const result=aw
 
 function gridRows(){const q=state.gridSearch.trim().toLowerCase(),rows=state.grid?.rows||[];return rows.filter(r=>!q||`${r.name} ${r.eid} ${r.rdo}`.toLowerCase().includes(q))}
 function gridDates(){const start=state.grid?.startDate,end=state.grid?.endDate;if(!start||!end)return[];const out=[];let d=new Date(end+'T00:00:00'),min=new Date(start+'T00:00:00');while(d>=min){out.push(localDateISO(d));d.setDate(d.getDate()-1)}return out}
-function renderGrid(){const rows=gridRows(),dates=gridDates();return `<div class="page-head"><div><div class="title">Rolling 9-Month Grid</div><div class="sub">Present days are intentionally hidden to keep the grid readable. Off/leave, negative attendance, and earned positive attendance points remain visible.</div></div><div class="actions no-print"><button onclick="go('daily')">Daily Entry</button><button onclick="go('roster')">Roster</button></div></div><div class="card"><div class="toolbar"><div><label>Ending Date</label><input type="date" value="${esc(state.gridEnd)}" onchange="changeGridEnd(this.value)"></div><div class="grow"><label>Search Name / EID / RDO</label><input class="search" value="${esc(state.gridSearch)}" oninput="state.gridSearch=this.value;render()" placeholder="Filter grid employees..."></div><div><span class="chip">${dates.length} calendar days</span> <span class="chip">${esc(fmtDate(state.grid?.startDate))} through ${esc(fmtDate(state.grid?.endDate))}</span></div></div></div><div class="table-wrap grid-wrap"><table class="nine-grid"><thead><tr><th class="grid-name">Employee</th>${dates.map(d=>`<th class="grid-date ${d===localDateISO()?'today':''}">${esc(shortDate(d))}<br><small>${esc(dayShort(d))}</small></th>`).join('')}</tr></thead><tbody>${rows.length?rows.map(r=>`<tr class="${thresholdClass(r.threshold)}"><td class="grid-name"><strong>${esc(r.name)}</strong><br><small>EID ${esc(r.eid)} · RDO ${esc(r.rdo||'')} · Net ${money(r.total)} · +${money(r.positiveBalance)} bank · ${r.cleanWorkedDays||0}/40</small></td>${dates.map(d=>{const c=r.cells?.[d];return `<td class="grid-cell ${['Sun','Sat'].includes(dayShort(d))?'weekend':''} ${c?'has-entry':''}" ${c?`onclick="openGridCell('${esc(r.eid)}','${esc(d)}')" title="${esc(c.label)}"`:''}>${c?`<span class="grid-code ${Number(c.positiveCredits)>0?'positive':''}">${esc(c.code||'•')}</span>`:''}</td>`}).join('')}</tr>`).join(''):`<tr><td class="muted">No employees match the current grid search.</td></tr>`}</tbody></table></div>`}
+function renderGrid(){const rows=gridRows(),dates=gridDates();return `<div class="page-head"><div><div class="title">Rolling 9-Month Grid</div><div class="sub">Present days are intentionally hidden to keep the grid readable. Off/leave, negative attendance, and earned positive attendance points remain visible.</div></div><div class="actions no-print"><button onclick="go('daily')">Daily Entry</button><button onclick="go('roster')">Roster</button></div></div><div class="card"><div class="toolbar"><div><label>Ending Date</label><input type="date" value="${esc(state.gridEnd)}" onchange="changeGridEnd(this.value)"></div><div class="grow"><label>Search Name / EID / RDO</label><input class="search" value="${esc(state.gridSearch)}" oninput="state.gridSearch=this.value;render()" placeholder="Filter grid employees..."></div><div><span class="chip">${dates.length} calendar days</span> <span class="chip">${esc(fmtDate(state.grid?.startDate))} through ${esc(fmtDate(state.grid?.endDate))}</span></div></div></div><div class="table-wrap grid-wrap"><table class="nine-grid"><thead><tr><th class="grid-name">Employee</th>${dates.map(d=>`<th class="grid-date ${d===localDateISO()?'today':''}">${esc(shortDate(d))}<br><small>${esc(dayShort(d))}</small></th>`).join('')}</tr></thead><tbody>${rows.length?rows.map(r=>`<tr class="${thresholdClass(r.threshold)}"><td class="grid-name"><button class="employee-name-link grid-employee-link" onclick="loadEmployee('${esc(r.eid)}')">${esc(r.name)}</button><br><small>EID ${esc(r.eid)} · RDO ${esc(r.rdo||'')} · Net ${money(r.total)} · +${money(r.positiveBalance)} bank · ${r.cleanWorkedDays||0}/40</small></td>${dates.map(d=>{const c=r.cells?.[d];return `<td class="grid-cell ${['Sun','Sat'].includes(dayShort(d))?'weekend':''} ${c?'has-entry':''}" ${c?`onclick="openGridCell('${esc(r.eid)}','${esc(d)}')" title="${esc(c.label)}"`:''}>${c?`<span class="grid-code ${Number(c.positiveCredits)>0?'positive':''}">${esc(c.code||'•')}</span>`:''}</td>`}).join('')}</tr>`).join(''):`<tr><td class="muted">No employees match the current grid search.</td></tr>`}</tbody></table></div>`}
 async function changeGridEnd(v){state.gridEnd=v||localDateISO();await refreshGrid();render()}
 function openGridCell(eid,date){const row=(state.grid?.rows||[]).find(r=>r.eid===eid),cell=row?.cells?.[date];if(!row||!cell)return;modal(`<div class="modal-head"><div class="modal-title">${esc(row.name)} · ${esc(fmtDate(date))}</div><button onclick="closeModal()">Close</button></div><div class="two-col"><div><strong>Attendance</strong><p>${esc(cell.label||cell.code)}</p></div><div><strong>Negative Points</strong><p>${money(cell.points)}</p><strong>Positive Earned</strong><p>+${money(cell.positiveCredits)}</p></div></div>${cell.notes?`<div class="notice"><strong>Notes:</strong> ${esc(cell.notes)}</div>`:''}<div class="modal-actions"><button onclick="closeModal()">Close</button><button class="primary" onclick="closeModal();loadEmployee('${esc(eid)}')">Open Employee Record</button></div>`)}
 
-function renderRoster(){const rows=rosterFiltered(),k=rosterStats();return `<div class="page-head"><div><div class="title">Warehouse Attendance Roster</div><div class="sub">Name, EID, RDO, and current net attendance total after positive credits.</div></div><div class="actions no-print"><button onclick="openEmployeeModal()">Add Employee</button><button onclick="printRoster()">Print Roster</button><button onclick="exportRoster()">Export Roster Excel</button><button class="primary" onclick="exportAll()">Export All Excel</button></div></div><div class="kpis"><div class="kpi"><div class="value">${k.people}</div><div class="label">Active Emplo